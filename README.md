# Password-generator
A Python program that generates cryptographically secure passwords using `secrets.choice()`.   Users can select uppercase, lowercase, digits, and symbols, and the program checks password strength.
